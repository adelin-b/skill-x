---
name: zod-base
description: Author runtime schemas and TypeScript-inferred types with Zod. Use whenever the user defines a validator, parses untrusted input (JSON body, env vars, query params, form data), needs a type derived from a runtime check, or asks "how do I validate X". Always trigger this skill before hand-writing a TypeScript interface for any value that crosses a trust boundary.
sources:
  - kind: npm
    package: zod
    range: "^3"
  - kind: docs
    url: "https://zod.dev"
  - kind: changelog
    url: "https://github.com/colinhacks/zod/releases.atom"
composed_from: []
compose_rule: ""
last_synced: "2026-05-06T19:28:59.902Z"
upstream_hash: "sha256:d09ce3c24a3323aeabf90a7cbfb8fbcbc366c32cf7848b4b235d161ccd2cdb07"
---

# zod-base (stub)

This is a primary skill. Its body is regenerated by `skill-sync` from upstream Zod docs / changelog.

The first run of `node tools/sync.mjs` will mark this skill `DRIFT` because `upstream_hash` is `~`. Hand it to the `skill-sync` skill in Claude Code; it will fetch the latest Zod docs, write the body here, and stamp `upstream_hash` + `last_synced`.

Until then, treat the body as empty.

## Stub workflow placeholder

1. Define a schema with `z.object({ ... })`.
2. Parse with `.parse(input)` (throws) or `.safeParse(input)` (Result-style).
3. Infer the static type with `z.infer<typeof schema>`.

(Real content arrives on first sync.)
