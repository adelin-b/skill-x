---
name: convex-base
description: Build Convex backend functions, schemas, queries, mutations, and actions. Use whenever the user works in a Convex project, defines a `convex/schema.ts`, writes a `query`, `mutation`, or `action`, configures `args` validators, or asks how to model data in Convex. Trigger proactively when files under `convex/` are edited.
sources:
  - kind: npm
    package: convex
    range: "^1"
  - kind: docs
    url: https://docs.convex.dev
  - kind: changelog
    url: https://github.com/get-convex/convex-js/releases.atom
composed_from: []
compose_rule: ""
last_synced: 1970-01-01T00:00:00Z
upstream_hash: ~
---

# convex-base (stub)

This is a primary skill. Its body is regenerated by `skill-sync` from upstream Convex docs / changelog.

First sync run will mark this skill `DRIFT`. Hand to `skill-sync` to fill in.

## Stub workflow placeholder

1. Declare schema in `convex/schema.ts` via `defineSchema`/`defineTable` and the `v.*` validators.
2. Write functions with `query`, `mutation`, `action` from `./_generated/server`.
3. Validate args with `args: { ... }` using `v.*`.

(Real content arrives on first sync.)
