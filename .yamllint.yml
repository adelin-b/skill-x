---
extends: default

rules:
  line-length:
    max: 200
  document-start: disable
  truthy: disable
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
  braces:
    max-spaces-inside: 1
